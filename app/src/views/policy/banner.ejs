<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Admin Page</title>
  <link rel="stylesheet" href="/css/home/policy/banner.css">
  <link rel="stylesheet" href="/css/home/layout.css">
  <script src="/ckeditor/build/ckeditor.js"></script>
</head>

<body>
    <%- include('../layout.ejs', { pageTitle: '배너 정보' }) %>
<section>
    <div class="register-btn" id="banner_reg_btn" onclick="location.href='/admin/policy/banner/upload'">
      <div class="register-text">등록</div>
    </div>
    <table id="banner_table">
        <thead>
            <tr>
                <th>번호</th>
                <th>제목</th>
                <th>링크</th>
                <th>등록일</th>
                <th>삭제</th>
            </tr>
        </thead>
        <tbody>
            <% if (banner.length === 0) { %>
                <tr>
                  <td colspan="10">조회된 데이터가 없습니다.</td>
                </tr>
              <% } else { %>
                <% for(var i=0; i<  banner.length; i++) { %>
                    <tr>
                        <td><%= i+1 %></td>
                        <td><%= banner[i].banner_name %></td>
                        <td><%= banner[i].banner_link %></td>
                        <td><%= `${banner[i].ins_date.getFullYear()}` + `-`+ `${banner[i].ins_date.getMonth()+1}` + `-`+ `${banner[i].ins_date.getDate()}`%></td>
                        <td><a href="/admin/policy/banner/delete/<%= banner[i].board_idx %>" onclick="return delBan()">삭제</a></td>
                        <script>
                        function delBan(){
                            if(confirm("정말 삭제하시겠습니까?")){
                            return true;
                            }else{
                            return false;
                            }
                        }
                        </script>
                    </tr>
                <% } %> 
            <% } %>
        </tbody>
    </table>
</section>
</body>
</html>