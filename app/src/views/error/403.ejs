<!-- FILEPATH: /c:/teenTalkTalk/app/src/views/error/403.ejs -->

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>403 Error</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            text-align: center;
            padding: 50px;
        }
        h1 {
            color: #333;
            font-size: 36px;
            margin-bottom: 20px;
        }
        p {
            color: #666;
            font-size: 18px;
            margin-bottom: 20px;
        }
    </style>
    <script>
        var requiredAccessLevel = <%= requiredAccessLevel %> ;
        if (requiredAccessLevel === 1) {
            alert("최고 관리자만 접근 가능합니다.");
        } else if (requiredAccessLevel === 2) {
            alert("부 관리자 이상 접근 가능합니다.");
        } else if (requiredAccessLevel === 3) {
            alert("정책 관리자 이상 접근 가능합니다.");
        }
    </script>
</head>
<body>
    <h1>403 Error</h1>
    <p>Access Denied</p>
</body>
</html>
